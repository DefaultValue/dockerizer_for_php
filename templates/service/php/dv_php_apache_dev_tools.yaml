version: '3.7'

services:
  php-apache:
    image: defaultvalue/php:{{php_version}}-development
    # Must add extra_hosts to be written to /etc/hosts - otherwise containerized application can not curl itself
    extra_hosts:
      # Required for xDebug on Linux hosts till docker.host.internal is not available by default - https://github.com/docker/libnetwork/pull/2348/files
      - "host.docker.internal:172.17.0.1"
    environment:
      # Go is required to catch emails with Mailhog and mhsendmail (Sendmail replacement)
      - ADDITIONAL_PATH=/usr/local/go/bin
      - PHP_IDE_CONFIG=serverName={{domains|first}}
      - COMPOSER_VERSION={{composer_version}} # Just for test
    volumes:
      - test/file_3.txt:file_3.txt

  mysql:
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_PASSWORD=root
# @TODO: REPLACE HERE AND FOR TRAEFIK! DOMAIN HERE MUST NOT CONTAIN DOTS!!!!!
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: always
    labels:
      - traefik.enable=true
      - traefik.http.routers.phpmyadmin-{{environment}}-{{domains|first|replace:.:-}}-http.rule=Host(`phpmyadmin-{{environment}}.{{domains|first}}`)
      - traefik.http.routers.phpmyadmin--{{environment}}-{{domains|first|replace:.:-}}-http.entrypoints=http
    depends_on:
      - mysql
    environment:
      - PMA_HOSTS=mysql
      - PMA_USER=root
      - PMA_PASSWORD=root
      - PMA_ABSOLUTE_URI=http://phpmyadmin-{{environment}}.{{domains|first}}/
    volumes:
      - /sessions

  mailhog:
    container_name: mailhog
    image: mailhog/mailhog:v1.0.1
    restart: always
    network_mode: bridge
    labels:
      - traefik.enable=true
      - traefik.http.routers.mailhog-{{environment}}-{{domains|first|replace:.:-}}-http.rule=Host(`mailhog-{{environment}}.{{domains|first}}`)
      - traefik.http.routers.mailhog-{{environment}}-{{domains|first|replace:.:-}}-http.entrypoints=http
      - traefik.http.services.mailhog-{{environment}}-{{domains|first|replace:.:-}}-http.loadbalancer.server.port=8025