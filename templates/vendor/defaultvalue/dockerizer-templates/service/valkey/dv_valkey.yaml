services:
  valkey:
    image: valkey/valkey:{{valkey_version}}
    restart: always
    healthcheck:
      test: "valkey-cli ping"
      interval: 5s
      timeout: 3s
      start_period: 10s
      retries: 10
